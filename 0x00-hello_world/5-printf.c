#include <stdio.h>

/**
 * main - A program that prints a sentence using printf followed by a new line.
 *
 * Return: Always return 0.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
